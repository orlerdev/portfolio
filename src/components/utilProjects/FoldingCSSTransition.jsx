import styled from '@emotion/styled'

const FoldingCSSTransitionContainer = styled.div``;

const FoldingCSSTransition = () => {
  return (
    <FoldingCSSTransitionContainer></FoldingCSSTransitionContainer>
  )
};
export default FoldingCSSTransition;